# RouteOn — 법정 휴게 삽입 로직 (확정 의도)

팀 공유용. **2시간 연속 운전** 한도를 기준으로 한 휴게소 삽입 제품 로직과, 현재 구현 갭·수정 방향만 기록한다.

---

## 제품 로직 (확정)

- **기준:** 연속 운전 **최대 2시간** (`MAX_DRIVE_SEC` 등 코드 상수와 동일 개념).
- **예시 시나리오 (의도된 동작):**
  1. 출발
  2. **상차 전 약 1시간** 연속 운전
  3. **상차** (적재·하역 등 — 휴게 불가 구간으로 취급할 수 있음)
  4. **남은 1시간**(2시간 한도에서 이미 쓴 1시간의 **잔여**) 안에 휴게소로 이동·휴식
  5. 이후 목적지(하차 등)로 이동

- **이월(잔여 시간 carryover):** 상차 직전까지 쓴 연속 운전 시간(예: 1시간)은 상차 **이후** 구간의 “이미 소모한 연속 운전”으로 **이어진다**. 즉 상차 후에는 “다시 2시간부터”가 아니라 **“남은 1시간 안에 휴게”**가 자연스럽다. 이 동작은 **버그가 아니라 확정된 제품 의도**다.

---

## 현재 구현 갭 (알려진 이슈)

| 현상 | 원인(요약) |
|------|------------|
| 상차 직후 휴게 타이밍이 어긋남 | `defer` 등: 상차 waypoint 근처(~20분) 휴게 제거 후 다음 구간 `initial_drive_sec ≈ MAX_DRIVE_SEC`(2시간 풀)로 설정 → **“1시간 남음”이 아니라 “이미 2시간 운전한 것”**처럼 동작 |
| 구간마다 휴게가 과다 삽입 | `segment_times`로 leg 단위 분할 + `REST_PLAN_SEC`(약 1시간 40분)마다 greedy 삽입 → 장거리(예: 양주–부산–진안, 8정류장)에서 휴게 과다 |
| 상·하차에서 누적 운전 시간 미반영 | `pickup`/`delivery` (`can_rest=False`)에서 제품이 기대하는 **연속 운전 누적기 리셋/이월**이 구현과 불일치 |
| `/demo/route` 폴리라인 경로 | 구 `insert_rest_stops`의 **“남은 leg ≤ 2시간이면 휴게 생략”** 규칙이 빠져 단차 데모와 제품 의도 불일치 |

---

## 수정 방향 (구현 가이드만, 코드 없음)

- 상차(또는 화물 waypoint) 시점에 **`accumulated_drive`를 실제 잔여 연속 운전 시간**으로 설정(리셋이 아니라 **이월 반영**).
- `defer`와 `initial_drive_sec`를 **“잔여 1시간”** 개념에 맞추고, **MAX_DRIVE_SEC 풀 부여**는 하지 않는다.
- leg·trip 단위로 **휴게 횟수 상한** 또는 남은 연속 운전 창 안에서만 삽입하도록 `REST_PLAN_SEC` greedy와 정합.
- 상차 후 **필요 시 남은 창 안 1회** 휴게 삽입 규칙을 단차·replan·GH 경로와 동일하게 맞춘다.
- `/demo/route`에도 **잔여 leg ≤ 2시간 시 휴게 생략** 규칙을 제품 로직과 동일하게 복원·정렬한다.

---

*다차량 배차·권역·사후 통계 등은 본 문서 범위 밖. API·스키마 개요는 [README.md](README.md), [SCHEMA.md](SCHEMA.md).*
